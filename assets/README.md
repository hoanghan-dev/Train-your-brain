# BrainArena Static Assets Directory

Thư mục lưu trữ tài nguyên tĩnh:
- `images/`: Hình ảnh logo, banner, avatar tùy chỉnh
- `icons/`: Icon SVG tùy chỉnh hoặc favicon
- `fonts/`: Font chữ cục bộ (nếu cần chạy hoàn toàn offline không Internet)
